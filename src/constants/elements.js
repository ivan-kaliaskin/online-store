const elements = {
    _pageContainer: null,
    set pageContainer(oPageContainer) {
        this._pageContainer = oPageContainer
    },
    get pageContainer() {
        return this._pageContainer
    }
}

export default elements